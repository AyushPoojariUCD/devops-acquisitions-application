name: Lint and Format Check

on:
  push:
    branches:
      - main
      - staging
  pull_request:
    branches:
      - main
      - staging

jobs:
  lint-and-format:
    name: Lint and Format Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        id: eslint
        run: |
          echo "::notice::Running ESLint check..."
          npm run lint || {
            echo "::error::ESLint found issues in your code"
            echo "::notice::Run 'npm run lint:fix' to fix auto-fixable issues"
            exit 1
          }

      - name: Run Prettier check
        id: prettier
        run: |
          echo "::notice::Running Prettier format check..."
          npm run format:check || {
            echo "::error::Code formatting issues found"
            echo "::notice::Run 'npm run format' to fix formatting issues"
            exit 1
          }

      - name: Success notification
        if: success()
        run: echo "::notice::All linting and formatting checks passed! ✅"
